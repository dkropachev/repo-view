Review commit {{COMMIT_SHORT}} compared with {{BASE}} for correctness regressions and missing tests. Account for the production implementation, unit tests, benchmarks, and changed performance documentation. Compare no-argument methods that previously consulted an injected service with the native behavior, and assess compatibility risk separately from demonstrated production failures. If the exact native dependency implementation is outside the inspected evidence, state that source boundary rather than inventing its clock calls. Check whether the public RateLimiter and Reservation method sets and the NewRateLimiter and NewDynamicRateLimiter constructor signatures changed; name the material embedded or delegated operations that remain unchanged. Trace representative concrete Reserve and ReserveN consumers when they are present in inspected evidence. Cover concrete construction paths in persistence health, matching, replication, and worker, scheduler, or migration code when present. Support impact claims with concrete inspected source paths, distinguishing construction from a proven consuming call chain. Assess whether allocation measurements are enforced by a failing assertion or merely reported, and whether successful delayed-reservation and cancellation behavior is covered. Report findings ordered by severity with file and line references. Give a scoped correctness conclusion without claiming semantic equivalence beyond the inspected evidence. Read only; do not modify files or run tests.
