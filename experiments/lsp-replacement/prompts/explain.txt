Explain changes in commit {{COMMIT_SHORT}} compared with its first parent ({{BASE}}). Describe the behavior change, affected code paths, tests, and performance rationale. Account for every changed artifact category, including production implementation, unit tests, benchmarks, and performance documentation. Cross-check all changed performance documentation before answering. Report every quantitative measurement it adds, including activity, write-admission, and read/drain throughput categories when present, and report the documented workflow, node-health, and error-validation outcomes. Do not stop after microbenchmark or allocation-profile results. When a wrapper is replaced, explicitly compare no-argument methods that previously consulted an injected service with the native behavior. If the exact native dependency implementation is outside the inspected evidence, state that source boundary rather than inventing its clock calls. State whether the public RateLimiter and Reservation method sets and the NewRateLimiter and NewDynamicRateLimiter constructor signatures changed; name the material embedded or delegated operations that remain unchanged. Trace representative concrete Reserve and ReserveN consumers when they are present in inspected evidence. Cover concrete construction paths in persistence health, matching, replication, and worker, scheduler, or migration code when present. Support downstream-impact claims with concrete inspected source paths rather than category labels alone, distinguish construction or interface-level impact from a proven consuming call chain, and state the unchanged scope supported by the evidence. Distinguish measured results from inference, and do not claim semantic equivalence beyond what the inspected evidence establishes. Read only; do not modify files.
